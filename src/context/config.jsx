import { createContext } from 'react'

/** This is the context holding the config story data from storyblok(business address, navigation links, footer content ...)  */
const configContext = createContext(null)

export default configContext
